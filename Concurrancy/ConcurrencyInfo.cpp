/**
 * HEADING: Concurency
 * DEFINITION: to run logic in parallel in a CPU 
 * 
 * std::thread
 * accepts a callable as an argument
 * callable is executed in that separate thread
 * thread constructor does not for main thread to start
 * 
*/